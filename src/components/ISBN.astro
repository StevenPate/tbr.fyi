---
const { book } = Astro.props;
const {
    data: { isbn, id },
} = book;
---

{!isbn && id && 
    <dt class="id">ID</dt>
    <dd id={id} data-id={id}>{id}</dd>
}
{isbn && 
    <dt class="isbn">ISBN</dt>
    <dd id={isbn} data-isbn={isbn}>{isbn}</dd>
}
