---
const { book } = Astro.props;
const { id, image = "/images/placeholder.png", title } = book;
---

{
    image && (
        <div class="cover" itemprop="cover">
            <img src={image} alt={title || id} />
        </div>
    )
}
